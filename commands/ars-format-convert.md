---
description: ARS academic-paper `format-convert` mode — convert to LaTeX / DOCX / PDF / Markdown
---

Codex invocation note for the `academic-paper` skill in `format-convert` mode. Converts a paper between LaTeX, DOCX (via Pandoc), PDF, or Markdown, and converts citation styles between major formats. Fidelity spectrum, low oversight.

Mode reference: `MODE_REGISTRY.md` § academic-paper.
Codex skill entry: `academic-paper/SKILL.md`.
