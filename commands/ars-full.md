---
description: ARS full pipeline — research → write → review → revise → finalize
---

Codex invocation note for the `academic-pipeline` orchestrator (`(pipeline)` in MODE_REGISTRY.md — the orchestrator has no named mode of its own). Load the skill and execute the complete academic research workflow (10-stage orchestration: deep-research → academic-paper → integrity → academic-paper-reviewer → revision → re-review → final integrity → finalize).

Mode reference: `MODE_REGISTRY.md` § academic-pipeline.
Codex skill entry: `academic-pipeline/SKILL.md`.
